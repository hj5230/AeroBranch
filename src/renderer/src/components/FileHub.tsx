import React from 'react'
import { Card } from 'antd'

class FileHub extends React.Component {
  render(): React.ReactNode {
    return (
      <>
        <Card></Card>
      </>
    )
  }
}

export default FileHub
